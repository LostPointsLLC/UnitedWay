
<!-- favorite object that has url, title, type and typeId -->
favObject = new Object();

<!-- function that is called when user clicks the favorite button -->
function getFavObject(href, title, type, typeId){
	favObject.url = href;
	favObject.title = title;
	favObject.type = type;
	favObject.typeId = typeId;
}

<!-- store favorite object into the database -->
function storeFav(userId, favObject){

<!-- output: none -->
}

<!-- get favorite objects array from the database to display it in the actual favorites list -->
function getFavArray(userId){

<!-- output: array of favorite objects -->
}

<!-- get favorite object when the user clicks on the actual favorite obejct in the list -->
function getFavorite(userId, type, typeId){

<!-- url if the type is news feed or the actual data if the type is other stuff -->
}

<!-- delete favorite object when user clicks on delete button -->
function deleteFavorite(userId, type, typeId){

<!-- output: none -->
}